# Approvals API (Express server, server-side only)
BACKEND_API_URL=http://localhost:3002

# WebSocket server (client-side — use Railway URL in production)
NEXT_PUBLIC_WS_URL=http://localhost:3002

# Approvals API (client-side direct fetch — can match BACKEND_API_URL in dev)
NEXT_PUBLIC_API_URL=http://localhost:3002

# Google Search Console credentials (per site)
GSC_OAUTH_SITE={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# Google Sheet Id
SHEETS_ID=