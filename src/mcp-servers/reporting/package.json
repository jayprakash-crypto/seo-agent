{
  "name": "@seo-agent/reporting",
  "version": "1.0.0",
  "description": "MCP server — Slack digest posting, Google Sheets logging, weekly digest formatting",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx watch server.ts",
    "test": "cd ../../../ && npm test -- --testPathPattern=reporting"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "express": "^4.18.2",
    "googleapis": "^140.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
